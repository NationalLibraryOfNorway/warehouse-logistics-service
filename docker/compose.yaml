# You can set the compose file to run and test the application locally in a docker container
# This is a good way to test the application, especially if you are using a database or other services
# It can easily be run with the command `docker-compose up` and set up a local test environment as well as run the app